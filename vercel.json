{
  "buildCommand": "cd react-client && npm install --prefix .. && npx webpack --mode production",
  "outputDirectory": "react-client/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
